#include <stdio.h>

/**
 * main - is the entry point
 *
 * I'm printing a string to the concole with the printf func
 *
 * Return: 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
