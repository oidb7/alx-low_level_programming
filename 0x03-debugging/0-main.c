#include "main.h"

/**
 * main - tests the function that prints if the integer is positive or else.
 *
 * Return: 0 on success
 */

int main(void)
{
	int n = 0;
	positive_or_negative(n);

	return (0);
}
